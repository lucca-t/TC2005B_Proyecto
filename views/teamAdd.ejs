<%- include('includes/_header') %>

<!-- THIS SECTION IS ENTIRELY VIBE CODED, ONLY FOR VISUAL PURPOSES -->

<section class="section">
    <div class="container">
        <div class="level mb-4">
            <div class="level-left">
                <h1 class="title">Team Management</h1>
            </div>
            <div class="level-right">
                <a href="/teams/list" class="button is-light"><strong>← Back</strong></a>
            </div>
        </div>

```
    <div class="columns">
        <!-- LEFT: TEAM INFO -->
        <div class="column is-5">
            <div class="box">
                <h2 class="subtitle has-text-centered mb-5">Create New Team</h2>

                <form action="/teams/add" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                    <div class="field">
                        <label class="label">Team Name</label>
                        <div class="control">
                            <input class="input" type="text" name="teamName" placeholder="Enter team name" required>
                        </div>
                    </div>

                    <div class="field mt-5">
                        <div class="control">
                            <button type="submit" class="button is-primary is-fullwidth">
                                <strong>Create Team</strong>
                            </button>
                        </div>
                    </div>
                </form>

                <div class="notification is-warning is-light mt-4">
                    ⚠ You must select between <strong>1 - 3 Lead Members</strong>
                </div>
            </div>
        </div>

        <!-- RIGHT: MEMBER SELECTOR -->
        <div class="column is-7">
            <div class="box">
                <h2 class="subtitle mb-4">Select Members</h2>

                <!-- SEARCH -->
                <div class="field mb-4">
                    <div class="control has-icons-left">
                        <input id="searchInput" class="input" type="text" placeholder="Search user by name...">
                        <span class="icon is-left">🔍</span>
                    </div>
                </div>

                <!-- TABLE -->
                <div style="max-height: 400px; overflow-y: auto;">
                    <table class="table is-fullwidth is-striped is-hoverable">
                        <thead>
                            <tr>
                                <th>User</th>
                                <th>Role</th>
                                <th>Select</th>
                            </tr>
                        </thead>
                        <tbody id="userTable">
                            <% if (typeof users !== 'undefined' && users.length > 0) { %>
                                <% users.forEach(user => { %>
                                    <tr>
                                        <td class="email"><strong><%= user.email %></strong></td>

                                        <td>
                                            <input type="checkbox" name="members" value="<%= user.id %>">
                                        </td>

                                        <td>
                                            <input type="checkbox" class="lead-checkbox" name="leads" value="<%= user.id %>">
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="3" class="has-text-centered has-text-grey py-5">
                                        No users available.
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

                <p class="has-text-grey mt-3">
                    Select members and mark exactly 3 as leads.
                </p>
            </div>
        </div>
    </div>
</div>
```

</section>

<!-- 🔥 JS PARA BUSCADOR Y VALIDACIÓN -->

<script>
    // 🔍 SEARCH FILTER
    const searchInput = document.getElementById("searchInput");
    const rows = document.querySelectorAll("#userTable tr");

    searchInput.addEventListener("keyup", function () {
        const value = this.value.toLowerCase();

        rows.forEach(row => {
            const email = row.querySelector(".email")?.innerText.toLowerCase() || "";
            row.style.display = email.includes(value) ? "" : "none";
        });
    });

    // 🔒 LIMIT LEADS TO 3
    const leadCheckboxes = document.querySelectorAll(".lead-checkbox");

    leadCheckboxes.forEach(cb => {
        cb.addEventListener("change", () => {
            const checked = document.querySelectorAll(".lead-checkbox:checked");

            if (checked.length > 3) {
                cb.checked = false;
                alert("Only 3 lead members allowed.");
            }
        });
    });
</script>


<%- include('includes/_footer') %>
<!-- THIS IS NOT DONE -->